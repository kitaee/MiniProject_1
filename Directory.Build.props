<?xml version="1.0" encoding="utf-8"?>
<Project>
  <PropertyGroup>
    <!-- Team SDK path: frameworks/nFramework.path (default C:\LIG\nFrameworkv1.8.2) -->
    <NFW_DIR>C:\LIG\nFrameworkv1.8.2\</NFW_DIR>
    <!-- Optional per-machine override: create Directory.Build.props.user with your own NFW_DIR -->
  </PropertyGroup>
  <Import Project="$(MSBuildThisFileDirectory)Directory.Build.props.user" Condition="Exists('$(MSBuildThisFileDirectory)Directory.Build.props.user')" />
</Project>
