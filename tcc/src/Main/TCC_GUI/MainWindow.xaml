<Window x:Class="TCC_GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TCC" Height="400" Width="640">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="DeployScenarioButton"
                Grid.Row="0"
                Content="Deploy Scenario (UDP)"
                Padding="12,8"
                HorizontalAlignment="Center"
                Click="DeployScenarioButton_Click"/>
        <TextBlock x:Name="StatusText"
                   Grid.Row="1"
                   Margin="0,12,0,0"
                   Text="Starting nFConnect..."
                   HorizontalAlignment="Center"
                   FontSize="13"
                   TextWrapping="Wrap"
                   TextAlignment="Center"/>
        <TextBlock Grid.Row="2"
                   Text="TCC — nFConnect + UDP multicast (239.255.0.1:10000)"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="12"
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   TextAlignment="Center"/>
    </Grid>
</Window>
